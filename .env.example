# Postgres connection used by drizzle-kit (migrations) and the seed script.
# Matches docker-compose.yml.
DATABASE_URL=postgres://odyssey:odyssey@localhost:5432/odyssey

# Base URL the dashboard uses to reach the backend Worker (wrangler dev default).
EXPO_PUBLIC_API_URL=http://localhost:8787
